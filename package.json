{
  "name": "jump-to",
  "version": "1.0.0",
  "description": "A Chrome Extension for copying headlines without formating on Aftenposten",
  "main": "main.js",
  "scripts": {
    "test": "tsc --noEmit && npx prettier --check src",
    "build-package": "mkdir -p dist && cp -r src dist/ && cp manifest.json dist/"
  },
  "author": "eeue56",
  "license": "Apache-2.0",
  "devDependencies": {
    "prettier": "^3.2.5",
    "prettier-plugin-jsdoc": "^1.3.0",
    "typescript": "^5.4.2"
  }
}
